#!/bin/bash

alias ,p="$EDITOR $BASH_FOLDER/*-path"

appendpath () {
    case ":$PATH:" in
        *:"$1":*)
            ;;
        *)
            PATH="$1:$PATH"
    esac
}

appendpath "$HOME/git/scripts"
appendpath "$HOME/git/scripts/not_mine"
appendpath "$HOME/bin"
appendpath "$HOME/.local/bin"
