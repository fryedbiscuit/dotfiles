#!syntax bash
# Reset sxhkd
alt + shift + s
	pkill -10 sxhkd

# Open terminal
alt + Return
	$TERMINAL

# Kill window manager
alt + ctrl + q
    logout-wm

# Open terminal with dvtm
alt + Shift_R
    $TERMINAL -e tmux new -A -s session

# Open text editor with sxhkrc
alt + shift + slash
	$TERMINAL -e nvim ~/.config/sxhkd/sxhkdrc

# Force kill current program
alt + shift + q
	kill -9 `xdotool getwindowfocus getwindowpid`

# Open program
alt + d
	dmenu_history -l 20

# Open browser
alt + w
	chromium-browser

# Open pulsemixer
alt + a
	$TERMINAL -e pulsemixer

# Open htop
alt + shift + h
	$TERMINAL -e htop

# Open Vifm
alt + v
	$TERMINAL -e vifm

# Open PCManFm
alt + shift + v
	pcmanfm

# Escape key emulation
@XF86WWW
	xdotool key Escape
